const SizeField = ({size, value}) => {
    return (
      <div className="size-field-card">
        <span className="size-field-text">{size}</span>
      </div>
    );
  };
  
  module.exports = SizeField;